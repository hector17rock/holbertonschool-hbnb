// Configuration file for API endpoints
// Replace with your actual API URL when available

const API_CONFIG = {
    BASE_URL: 'https://your-api-url',
    ENDPOINTS: {
        LOGIN: '/login',
        LOGOUT: '/logout',
        PLACES: '/places',
        REVIEWS: '/reviews'
    }
};

// For testing purposes, you can update the API URL here:
// API_CONFIG.BASE_URL = 'http://localhost:5000'; // Example for local development
